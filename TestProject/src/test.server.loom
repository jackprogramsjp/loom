interface EventObject {
    event e(param: string);
}

fn on_e(p: string): void {
    print("param: " + p)
}

let eo = none as never as EventObject;
eo.e += on_consumer;
eo.e("abc");
eo.e -= on_consumer;