enum Empty { }

enum Abc { A, B, C }

enum Mixed { A, B = 69, C }

enum Status { Active = 1, Inactive = 0, Pending = 2 }

enum Colors : string {
    Red = "FF0000",
    Green = "00FF00",
    Blue = "0000FF"
}